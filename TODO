Need for For v1.0:

1, Update notification { Changelog ?  }

2, Option Page {
  remote vromerc support
  textarea?
}

// Share the same UI
3, Add support for open Bookmark/History
4, cs subdomain -> magic mode ;) create some subdomain let use choose one from it)
5, ch hostname


TODO ?

CmdBox { transparent ?  }
Basic help page

readability
save/list session
auto refresh
prevent popup in insert mode. (fix ? for twitter)
omni box


DEPRECATED:
  getSelected() have been deprecated in chrome16, use chrome.tabs.query()
  You are no longer required to specify the tabID for the chrome.tabs.update() method. When not provided, the tabID defaults to the selected tab of the current window.  


NO AVAILABLE API:
  ~   Open home directory
  gh  goto home page
