Basic help page

Update notification {
  Changelog ?
}

Option Page {
  remote vromerc support
  textarea?
}

CmdBox {
  transparent?
}

### Navigation ###
cs subdomain

### Bookmark ###
visit

readability ?


### Command Mode ###
save/list session ?
auto refresh ?

prevent popup in insert mode. (fix ? for twitter)

omni box (?)


DEPRECATED:
  getSelected() have been deprecated in chrome16, use chrome.tabs.query()
  You are no longer required to specify the tabID for the chrome.tabs.update() method. When not provided, the tabID defaults to the selected tab of the current window.  


NO AVAILABLE API:
  ~   Open home directory
  gh  goto home page
